"""Episodic entity (name + description + search fields) generation."""

EPISODE_ENTITY_PROMPT = """
你是情节记忆专家。针对以下对话，输出一个 JSON 对象，定义该情节（episode）的标题、事实性摘要和可检索短语。

对话时间戳: {conversation_timestamp}
对话内容:
{conversation_text}

说话人说明：对话行可能使用 `speaker=Name: ...` 表示具名说话人。请将 `Name` 视为该行真实说话人；该行中的第一人称陈述属于 `Name`，不要自动归因到用户。如存在显式说话人姓名，应使用该姓名。

返回一个 JSON 对象，仅包含以下字段：
{{
    "title": "简洁、描述性的标题（10-20字），准确概括主题",
    "content": "对主要话题和关键事实的简要事实性摘要（2-4句话），以第三人称书写",
    "search_fields": ["最多 {max_fields} 条简短检索短语（每条5-20字）"]
}}

## title 与 content 要求
1. 标题应具体且易于检索，包含关键话题、活动和参与者姓名。
2. content 应简要概括主要话题和关键事实，使用第三人称。保持简洁——不要复述整段对话。
3. 聚焦可检索要素：谁在什么时间、什么地点做了什么，以及为什么。
4. 包含所有专有名词：人名、地名、品牌名、产品名、书名/电影名。
5. 包含关键数字、日期、时间、数量和价格。
6. 相对时间使用双时间格式："相对时间（基于 {conversation_timestamp} 的绝对日期）"。
7. 一致使用具体姓名而非代词。
8. 去除寒暄、问候和冗余。
9. 保留因果关系和决策依据。
10. content 应显著短于原始对话——是简要概述，而非详细复述。

## search_fields 要求
1. **多样性**：每条字段应覆盖该情节的不同方面或角度，避免冗余。
2. **面向查询**：以用户自然搜索的方式书写短语。
3. **全面覆盖**：覆盖谁、什么、何时、何地、为何，以及提到的具体姓名、物品、计划和结果。
4. **自包含**：每条短语应独立可理解，无需依赖其他字段。
5. **包含关键姓名**：字段中提及主要参与者以消除歧义。
6. 当情节事实多于字段预算时，将相关信息归并为更高层的概括短语。

只返回 JSON 对象，不要输出任何其他文本。
"""
